[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
version = "0.13.4"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/kovvbojAV/cuePool"
rust-version = "1.97"

[workspace.dependencies]
# Core
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
anyhow = "1.0"
log = "0.4"
rust_decimal = { version = "1.36", features = ["serde"] }
flate2 = "1"
roxmltree = "0.20"
gethostname = "1.1"

# Audio / Video
cpal = "0.18.1"
rubato = "0.16"
symphonia = { version = "0.6", features = ["all"] }
ffmpeg-next = "9.0"
hap-parser = "0.2.0"

# GPU / UI
# frame-pacing-diag is a fork-only feature (see [patch.crates-io]): fence-lock
# timing for the Help → Status "Frame Pacing (wgpu)" section, built for the
# #139 delivery-stall investigation. Trivial overhead; kept on in field builds.
wgpu = { version = "30.0", features = ["frame-pacing-diag"] }
winit = "0.30"
egui = "0.36"
egui-wgpu = "0.36"
egui-winit = "0.36"
eframe = "0.36"
pollster = "0.3"

# Networking / Protocols
rosc = "0.11"
# Enumerates local interfaces for the OSC destination picker. Posix + Windows;
# used only to build the dropdown, never to bind a socket.
if-addrs = "0.15"
tokio = { version = "1.42", features = ["rt-multi-thread", "sync", "net", "time", "fs"] }
axum = { version = "0.8", features = ["ws"] }
tokio-stream = { version = "0.1", features = ["sync"] }
utoipa = "5.5"

# MIDI
midir = "0.11"

# Threading / Sync
crossbeam = "0.8"
ringbuf = "0.5.2"

# Utils
bitflags = "2.9"
chrono = { version = "0.4", features = ["serde", "std", "clock"] }
dirs = "6.0"
rfd = "0.15"
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }

# Engine crates, consumed from crates.io — CuePool's last code tie to the
# engine tree. Local edits to crates/rustjay-lighting no longer reach CuePool
# until published; bump the version here to pick them up.
rustjay-lighting = "0.1.0"

# Internal crates (path deps managed by cargo workspace)
cuepool-core = { path = "crates/cuepool-core" }
cuepool-audio = { path = "crates/cuepool-audio" }
cuepool-gui = { path = "crates/cuepool-gui" }
cuepool-video = { path = "crates/cuepool-video" }
cuepool-protocols = { path = "crates/cuepool-protocols" }
cuepool = { path = "crates/cuepool" }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"

# wgpu-30 fork carrying only the frame-pacing-diag feature (branch
# cuepool/frame-pacing-diag-30). The v29-era interop patches are gone: wgpu 30
# provides the DX12 resource import and fence staging natively, and the #139
# fence-lock split landed upstream (gfx-rs/wgpu#9475).
[patch.crates-io]
wgpu = { git = "https://github.com/scroix/wgpu", rev = "0316ceebc36f89e48f71dae67144cffce3f11941" }
wgpu-core = { git = "https://github.com/scroix/wgpu", rev = "0316ceebc36f89e48f71dae67144cffce3f11941" }
wgpu-hal = { git = "https://github.com/scroix/wgpu", rev = "0316ceebc36f89e48f71dae67144cffce3f11941" }
wgpu-types = { git = "https://github.com/scroix/wgpu", rev = "0316ceebc36f89e48f71dae67144cffce3f11941" }
